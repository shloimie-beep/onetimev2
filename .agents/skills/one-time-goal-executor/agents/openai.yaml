interface:
  display_name: 'One Time Goal Executor'
  short_description: 'Execute one Git-controlled goal track with evidence'
  default_prompt: 'Use $one-time-goal-executor to verify and execute one conductor-assigned track from the current One Time goal, preserve the canonical Git control model, close provider or visual work through objective readback, and return a sanitized handoff.'
