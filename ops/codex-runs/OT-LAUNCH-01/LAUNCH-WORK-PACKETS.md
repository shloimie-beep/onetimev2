# OT-LAUNCH-01 launch work packets

These are execution prompts, not status. `ops/goals/OT-LAUNCH-01/BOARD.yaml` remains the only current status map. The conductor assigns a track before any separate Codex task starts and semantically integrates terminal handoffs.

## Common start contract

Use this prefix for every packet:

> Continue the existing OT-LAUNCH-01 goal in `shloimie-beep/onetimev2`. Read `AGENTS.md`, then `ops/goals/CURRENT.yaml`, the referenced `GOAL.md`, `SPEC.yaml`, `ACCEPTANCE.yaml`, `BOARD.yaml`, and `DECISIONS.yaml` completely. Read the assigned handoff schema and use `.agents/skills/one-time-goal-executor`. Verify that BOARD assigns exactly the named track and write scope to this task; otherwise return a proposed assignment without editing. Do not create another goal, Board, status model, provider path, payment ledger, or broad release. Do not edit BOARD as a non-conductor. Preserve clean worktrees, use scoped formatting, run focused tests, return a sanitized handoff, and stop at the first exact protected-value, human-provider, or production-promotion boundary.

## MAIN-LANDING-POLISH

Assigned track: `main_landing_polish`

> Implement LANDING-001 and LANDING-002 on the exact conductor-assigned source. The main hero title is “Worldwide Mishnah Learning” / “Live from Eretz Yisrael”; the smaller supporting line is “Give your son a love for learning Torah.” with one Sign Up Now CTA. Move the Jerusalem Rosh Hashanah countdown into document flow below the header and update it at load/day rollover; remove the fixed ticker and the complete WhatsApp/offline helper. Equalize outcome media, use a tested Retention focal crop, add no-JS/reduced-motion-safe reveal, put Ready-to-Run before How It Works, add accessible autoplay/pause behavior, make publication logos large equal white cards without the old label, use square full-source Rabbi media, and keep landing accents yellow/black/white. Preserve static public architecture, Tisha pages, consent, login/signup, zero real submissions, CSS <= 35,000 bytes, public JS <= 45,000 bytes, LCP <= 2.5s, CLS <= 0.1, and all recorded viewports.

## OPERATOR-LAUNCH-DASHBOARD

Assigned track: `operator_launch_dashboard`

> Implement OPS-LAUNCH-001 as an Admin-only Launch Status route/card/navigation entry using canonical brand primitives. Generate a read-only projection from BOARD and acceptance IDs with the exact BOARD source hash. Show current milestone, completed/total and objective percentage, latest sanitized change, works now, exact blockers, safe links, and one next executable task. Do not hand-maintain status or expose credentials, private destinations, customer data, provider links, or diagnostics. Prove non-admin denial, desktop/mobile discovery, deterministic generation, and stale-hash rejection.

## GHL-ACCESS-ENTITLEMENT-PROJECTION

Assigned track: `ghl_access_entitlement_projection`

> Implement ACCESS-001. GHL is the only ledger for payment transactions, invoices, subscription history, refunds, and payment operations. One Time stores only the account/product access projection required to authorize roles: state, effective and optional expiry timestamps, source reference, and revocation reason. Add an explicit free-pilot source that never fabricates a paid transaction. Reconcile and preserve immutable historical Stripe-test migrations but remove Stripe from the live product path. Consume canonical HighLevel events through the existing outbox/dispatcher with dedupe, replay safety, audit readback, and fail-closed missing/expired/revoked/ambiguous authorization. Use the next globally free forward-only migration only after checking every current source/ledger.

## VIDEO-TO-CLASSROOM-E2E

Assigned track: `VIDEO-TO-CLASSROOM-E2E`

> Implement VIDEO-E2E-001. Admin selects a canonical class occurrence and uploads to durable protected storage with a non-secret opaque locator. One restart-safe idempotent worker advances probe -> conservative trim -> transcription -> captions -> editable metadata/questions -> private Vimeo -> Admin review without duplicate transcription/upload. Approve, publish, unpublish, and revoke only for learners enrolled in that occurrence; deny unrelated siblings. Prove the complete synthetic provider-off path first. The separately reviewed operator-owned OpenAI/private-Vimeo canary may run only after retry/restart and occurrence isolation pass.

## ZOOM-STAGING-CANARY

Assigned track: `zoom_real_control_operator_change_set`

> Execute only the accepted reviewed provider-action job referenced by BOARD. Validate protected prerequisites without printing values; rotate/revoke the exposed staging class target; allowlist only the exact persistent-staging origin; configure canonical Meeting SDK and S2S/host/meeting values; keep every gate off until readback passes. Run one bounded human host plus fictional Student canary for join, Ready, consent, mute/unmute request, spotlight/reset, replay denial, and cross-student denial. Sanitize evidence and return real/canary gates to the reviewed safe state. Do not touch production Zoom, customer meetings, messages, or OBS; OBS is not required for Meeting SDK control proof.

## PRODUCTION-CONVERGENCE

Assigned track: `production_pilot`

> Build PROD-PILOT-001’s immutable semantic candidate. Compare the actual deployed production Git source, the current production release branch, and the accepted full-app staging source; never replace one branch mechanically or trust a stale APP_VERSION label. Preserve production-only Tisha fixes and full-app-only Admin/Parent/Student/content/Zoom/HighLevel work. Prove exact migrations/checksums, source uniqueness, build/test/browser gates, production backup/restore path, rollback commit, web/worker source agreement, and fail-closed providers. Do not deploy production from this packet; return the exact promotion handoff.

## PRODUCTION-ADMIN-AND-FREE-PILOT

Assigned track: `production_pilot`

> After the immutable production candidate is deployed by its separately reviewed promotion job, safely activate or reset the operator’s real Administrator identity and create one operator-owned free-pilot Parent/Student cohort. Never print credentials or use a synthetic payment. Prove Dashboard, Parent, Student, Classroom, approved playback on another device, access revocation, and unchanged unrelated accounts. Complete only the bounded login challenge required by the protected identity flow.

## PUBLIC-LEAD-CAPTURE-BOT

Assigned track: `public_lead_capture_bot`

> After GHL is available, implement LEAD-001 through one reviewed GHL website-chat job. Reuse the canonical OT-A1/KB/fields/tags/senders, consent and dedupe the lead, save/reopen/read back, and run one operator-owned bounded canary. Keep provider-off truth until configured. Do not restore the placeholder WhatsApp helper, infer newsletter consent, send broadly, or use Telegram as the public sales/support inbox. Only explicitly classified substantive Torah questions may enter the private Rabbi Telegram queue.

## VOICE-LEAD-BOT

Assigned track: `voice_lead_bot`

> This packet is deferred until the website lead assistant and controlled production pilot are accepted. Define telephony provider, consent/recording disclosure, business hours, escalation, transcript ownership, test number, bounded operator canary, and fail-closed states before any call.
