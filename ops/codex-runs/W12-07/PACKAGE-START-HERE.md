# W12 — One Time completion and BNA separation wave

This package is the authoritative launcher for the next parallel work wave. It was prepared after a read-only review of `webcraft-media/onetimev2`, PR #61, the current One Time release evidence, and the committed BNA operating sources.

## What this package solves

1. A fresh ChatGPT chat can be brought up to date without replaying the old conversation.
2. Multiple Codex windows can work independently without sharing a window or relying on local chat memory.
3. Every lane must preserve its prompt, progress, blockers, and resume instructions in GitHub.
4. Missing optional credentials no longer stop safe provider-independent implementation.
5. One final integrator—not the feature agents—owns shared files, migration renumbering, staging convergence, and launch proof.

## Important current truth

- A new ChatGPT chat does **not** automatically read the repository, `AGENTS.md`, Downloads, dirty worktrees, or prior Codex windows.
- Codex normally reads the applicable `AGENTS.md` after it opens the correct repository/worktree.
- The durable memory must live in pushed GitHub files. Run `W12-00` first so future chats have a canonical supervisor handoff.
- The current app has a fast, usable shell and substantial domain code. It does **not** yet prove that real Rabbi contacts, historical email/WhatsApp messages, protected Vimeo/Buffer canaries, or live Telegram/WhatsApp configuration are complete.
- Parent and student routes exist, but the operator still needs separate synthetic test identities and a complete visible test journey.
- Do not paste bot tokens, provider keys, passwords, personal phone numbers, private message bodies, or spreadsheets into ChatGPT, Codex prompts, Git, screenshots, or evidence.

## Run now in parallel

Open separate Codex windows for `W12-00` through `W12-08`. Copy the matching launcher from `02-COPY-PASTE-LAUNCHERS.md`. These lanes may run while other release work is in progress because they prohibit production deployment, broad sends, real bulk imports, and destructive provider mutations.

Optionally run the three GPT Pro audits `BNA-SEP-P1`, `BNA-SEP-P2`, and `BNA-SEP-P3` in parallel. Each must return its own clearly named ZIP with a self-contained Codex prompt.

## Do not run yet

Do **not** run `W12-99` until every desired W12 feature branch has pushed a draft PR and the active production/release lane has finished. `W12-99` is the semantic convergence, staging, dry-run import, and canary gate.

## Branch ownership summary

| Lane | Outcome | Primary ownership |
|---|---|---|
| W12-00 | Canonical director handoff | `ops/director/**`, supervisor guidance |
| W12-01 | Real-audience inventory and safe CRM import tooling | import/reconciliation domain, scripts, tests |
| W12-02 | Historical communications ingestion and premium communications UI | communications adapters/domain/UI |
| W12-03 | Parent/student test lab and complete journeys | portal routes/UI/fixtures/tests |
| W12-04 | Content, Vimeo, knowledge, and classroom vertical slice | content/classroom/provider seams/UI |
| W12-05 | One Time Rabbi/admin Telegram operations bot | Telegram runtime, capabilities, audit |
| W12-06 | Public WhatsApp lead assistant | WhatsApp runtime, lead persistence, handoff |
| W12-07 | Premium landing correction | public landing/assets/styles/visual tests |
| W12-08 | Admin dashboard and classroom productization | admin dashboard/class detail UI and APIs |
| W12-99 | Final semantic convergence and staging verification | all shared hotspots and release proof |

## Shared hotspots

Feature agents do not own these files as final authority: root package files, lockfiles, shared server composition, root exports, shared navigation, shared design-token entrypoints, migration numbering/ledger, and CI workflows. If a lane needs a provisional edit there, it must list it in `HOTSPOTS.json`. `W12-99` reconciles them manually.
