# OPS-03 Local Handoff

The staging login credentials are local-only and intentionally untracked.

Private handoff file:

`C:\Users\User\.ops03-runtime\OPS-03\ops03-handoff\OPS-03-LOGIN-HANDOFF.private.json`

Use this file locally for acceptance testing. Do not commit it, paste it into chat, attach it to a PR, or expose its values in logs.

The seeded accounts are fictional `example.test` users only.
