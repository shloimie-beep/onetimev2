# OT-74 Original Prompt

Source file:

`C:\Users\User\AppData\Local\Temp\CODEX-NOW-03-OT74-AUDIENCE-IMPORT (1).md`

Source SHA-256:

`8C30A267E2B9A6BFFB6C6BDF6A364F8B89979ADE0B02B7FEB4B07B023ED49F54`

Captured at: `2026-07-15T09:34:16+03:00`

```text
TASK: Build the safe legacy-audience reconciliation foundation.
REPOSITORY: webcraft-media/onetimev2
IMMUTABLE BASE: dfef7de2035e08f1ee72e0133ccf656fe7a74444
BRANCH: codex/ot74-audience-reconciliation
PRODUCTION IMPORT/SEND/DEPLOYMENT: FORBIDDEN

Treat the opening directory as untrusted and never modify/run BNA. Create a
clean One Time worktree from the exact base and persist an OT-74 execution
packet before work. Stale OT-60R control metadata is non-blocking.

Own feature-local audience/tag/reconciliation domain paths, migration namespace
1200-1299 after verifying it is free, feature-local APIs/components left
unmounted, unique import/dry-run tools, synthetic fixtures/tests and evidence.
Do not edit `app.ts`, AppShell, central CRM entry, shared barrels, root packages,
provider workers or OT-71/72 paths. OT-80 will mount the hooks.

Model facts independently:

- a contact can simultaneously be a lead, exist in the old system and be an
  active legacy user;
- school submissions are school leads requiring human follow-up and receive no
  class/portal entitlement;
- preserve source spreadsheet, batch, row fingerprint and import provenance;
- reconcile normalized email/phone only inside the One Time account;
- never merge by name alone;
- ambiguous matches go to manual review;
- imports/dry runs/replays are idempotent, audited and non-destructive;
- derive segments including migration-invite eligible, active legacy user,
  manual review, school follow-up and do-not-contact;
- preserve consent, suppression and communication eligibility independently;
- provide rollback/reconciliation records without destructive contact deletion.

Build parsers/mappers for CSV/XLSX-shaped normalized inputs using synthetic
fixtures. Do not ingest real spreadsheets or print row contents. Produce a
dry-run report with counts/reasons only. Prepare feature-local CRM tag/segment
contracts and components, but do not mount central UI or send any campaign.

Test duplicate inputs, conflicting identities, same-name people, missing email,
phone normalization, replay, rollback, cross-account isolation, archived and
suppressed contacts, school handling and 10k synthetic rows. Use PostgreSQL 16
CI where available; lack of local PostgreSQL blocks only that proof.

Checkpoint/commit/push each phase, open a draft PR against OT-60R, and leave
exact OT-80 wiring instructions and `RESUME.md`. No external sends or data
mutation.
```
