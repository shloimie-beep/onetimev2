# DIRECT CODEX PROMPT - OT-73 LANDING INTENT RECONCILIATION

```text
TASK: Implement the high-confidence One Time landing corrections now.
REPOSITORY: webcraft-media/onetimev2
IMMUTABLE BASE: dfef7de2035e08f1ee72e0133ccf656fe7a74444
BRANCH: codex/ot73-landing-intent-reconciliation
DEPLOYMENT/PRODUCTION/PROVIDER/DNS: FORBIDDEN

Treat the opening directory as untrusted. Never run or modify BNA. Locate/fetch
the One Time repository, create a clean isolated worktree from the exact base,
and persist this prompt under `ops/execution/ot-73/**`. Stale OT-60R control SHAs
are non-blocking.

Own only public landing/signup content, public landing components/scripts/CSS,
landing-only tests, and OT-73 evidence. Do not edit authenticated AppShell,
server composition, auth, CRM, portals, provider code, migrations, shared
barrels, or root package files.

Implement this exact contract:

1. Remove the entire visible yellow campaign band containing
   `JOIN FREE UNTIL ROSH HASHANAH`. Remove its DOM, deadline/client behavior,
   dead CSS, layout height and stale tests; do not merely hide it. Header flows
   directly into hero with no blank strip.
2. Do not replace it with a price, `$67`, trial, `free until`, or other offer in
   the header/hero.
3. Preserve visible black One Time logo/lockup, `Member Login` -> `/login`,
   header `Sign Up Now` -> `/signup`, and hamburger. At 360x800 and 390x844 all
   four remain visible, usable and unobscured without horizontal overflow.
4. Replace blanket mobile hiding of brand text with a compact layout. Preserve
   the brand title/subtitle where physically possible without deleting required
   actions.
5. Preserve exact hero copy:
   - `Worldwide Mishnah learning - live from Eretz Yisrael`
   - `Give your son a love for learning Torah.`
   - `Live every day at 7:00 p.m. Israel time.`
   - CTA `Sign Up Now`
   One upper-aligned text column, no extra marketing paragraph, no yellow hero
   prose, and hero CTA fully visible before scrolling at 360x800 and 390x844.
6. Remove yellow styling from the current `Secure student portal` bullet. Keep
   bullet/body copy readable white or pale blue until an exact yellow allowlist
   exists. Preserve yellow only for established CTA/icon/focus/brand accents.
7. Remove the public internal disclaimer saying the Accomplishment copy is not
   operator-certified. Keep Toronto as the Accomplishment image.
8. Use one consistent landing/signup footer: white-on-black One Time logo, exact
   line `One Time Mishnayos with Rabbi Eli Scheller.`, then links in order Home,
   Sign Up Now, Privacy, Terms, Member Login. Clean mobile stack; no placeholders
   or BNA branding.
9. Preserve section order: Header, Hero, What You Receive, What He'll Gain, How
   It Works, Who It's For, Rabbi/gallery/press, Final CTA, Footer.
10. Preserve the current single `Live Daily Mishnayos` panel and six bullets for
    this pass. Do not invent the unresolved replacement copy or convert it to
    eight tiles. Preserve exactly three large CTA buttons: header, hero, final.
11. Preserve signup/lead behavior, Toronto asset, bundle separation, card
    consistency, accessibility and performance.

Create a before/after copy-and-placement ledger with source evidence. Record
unrecoverable copy requests as pending, but do not let them block the confirmed
edits above. Capture screenshots at 360x800, 390x844, 768x1024 and 1440x900.
Assert no ticker/price text/dead listener/gap, header actions present, CTA above
fold, no overflow, correct footer, and existing lead tests green.

Commit/push, open a draft PR to `codex/ot60r-recovery-convergence`, and leave a
self-contained resume packet. No deployment.
```
