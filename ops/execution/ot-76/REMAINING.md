# OT-76 Remaining

- OT-80 or a later integrated release must provide a release manifest that
  replaces current `partial` and `missing_current_release_evidence` capability
  statuses with direct evidence.
- The future manifest should attach fresh integrated run results for:
  class/reminder boundaries, content review/publish, entitled library, provider
  default-off action truth, visible action registry mapping, RTL, 200 percent
  reflow, reduced motion, 30-sample performance, rollback, and readiness.
- Run `certify` only when the integrated release claims Day-One readiness.
