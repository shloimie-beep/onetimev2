schema_version: 1
execution_id: OT-V21-PRODUCTION
authority_id: ${AUTHORITY_ID}
task_id: ${TASK_ID}
candidate_digest: ${CANDIDATE_DIGEST_OR_NOT_APPLICABLE}
canonical_path: ops/v2.1-execution/authority/${CANDIDATE_OR_NONE}/${TASK_ID}/${AUTHORITY_ID}/GRANT.yaml
grant_status_at_issue: active
approved_by: ${APPROVER_IDENTITY}
approval_evidence_digest: ${APPROVAL_RECORD_SHA256}
scope: ${EXACT_SCOPE}
fencing_token: ${OPAQUE_FENCING_TOKEN}
lock: ${EXACT_PROVIDER_OR_INFRA_LOCK}
provider_account_location_or_asset_id: ${EXACT_NON_SECRET_IDENTITY}
allowed_operation: ${EXACT_OPERATION}
maximum_effect_count: ${COUNT}
maximum_effect_value: ${VALUE_OR_NOT_APPLICABLE}
approved_fixture_ids: []
operator_identity: ${OPERATOR}
approved_at: ${UTC_TIMESTAMP}
expires_at: ${UTC_TIMESTAMP}
cleanup_and_reconciliation: []
stop_conditions: []
contains_secret: false
immutable: true
