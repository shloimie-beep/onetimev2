# OT-75 Checkpoint

Current checkpoint: static preparation files are added, locally validated, and
published in draft PR #20:
https://github.com/webcraft-media/onetimev2/pull/20

OT80 conductor note: when OT75 is validated after merge into a convergence
branch, use `--scope-base` or `OT75_SCOPE_BASE_SHA` set to the OT75 merge
commit's first parent. The standalone OT75 branch still validates against the
immutable OT60R base by default.

No deployment, DNS, database, provider, send, payment, real-user, or BNA
mutation has been performed.
